{
  "scripts": {
    "prisma:migrate": "prisma migrate dev --name init_schema",
    "prisma:generate": "prisma generate",
    "prisma:seed:manual": "ts-node prisma/seed.ts"
  },
  "prisma": {
    "seed": "ts-node prisma/seed.ts"
  },
  "devDependencies": {
    "prisma": "^7.2.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "@prisma/client": "^7.2.0",
    "dotenv": "^17.2.3"
  }
}
